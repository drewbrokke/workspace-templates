import type {FDSCellRenderer} from '@liferay/js-api/data-set';

const fdsCellRenderer: FDSCellRenderer = ({value}) => {
	const element = document.createElement('div');

	element.innerText = `<b>${value}</b>`;

	return element;
};

export default fdsCellRenderer;
